# Required: Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Required: absolute path to your vault directory on the host
VAULT_PATH=/path/to/your/vault

# Optional: server port (default 3000)
# PORT=3000
