# Required: Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: where to store the vault on the HOST machine.
# Defaults to ./vault next to this file (auto-created on first run).
# VAULT_PATH=/absolute/path/on/host

# Optional: server port (default 3100)
# PORT=3100
